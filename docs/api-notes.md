# API Notes

This project is split between:

- non-root samplers using public Darwin and Mach interfaces
- optional root-only enhancement samplers

The default mode must remain useful without `sudo`.
