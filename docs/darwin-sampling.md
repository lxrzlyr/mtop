# Darwin Sampling

Current non-root sampling targets:

- Mach host CPU tick counters
- Darwin process task info
- sysctl topology and memory data
- IOKit battery state

Root-only enhancements will be isolated in a separate sampler layer.
